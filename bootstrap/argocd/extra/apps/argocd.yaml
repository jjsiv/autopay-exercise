apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: argocd
  namespace: argocd
spec:
  project: infrastructure
  destination:
    name: in-cluster
    namespace: argocd
  sources:
    - repoURL: https://argoproj.github.io/argo-helm
      chart: argo-cd
      targetRevision: 9.1.*
      helm:
        ignoreMissingValueFiles: true
        releaseName: argocd
        valueFiles:
          - $values/bootstrap/argocd/values.yaml
    - repoURL: https://github.com/jjsiv/autopay-exercise.git
      targetRevision: main
      ref: values
    - repoURL: https://github.com/jjsiv/autopay-exercise.git
      targetRevision: main
      path: bootstrap/argocd/extra
      directory:
        recurse: true
  syncPolicy:
    automated:
      selfHeal: true
      prune: true
    syncOptions:
      - ServerSideApply=true
      - CreateNamespace=true
