# Helm chart solution for KafkaTopic self-service

This Helm chart abstracts Strimzi KafkaTopic resource and provides validation through values.schema.json.

Example values files can be found in the `ci/` directory.
